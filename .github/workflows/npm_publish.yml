name: npm_publish
on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: read

jobs:
  publish_to_npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      # https://github.com/pnpm/pnpm/issues/9011
      # Git checks doesn't work for tag release yet
      - run: pnpm publish --no-git-checks --provenance --access public dist/
